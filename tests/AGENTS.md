# Test Directory Agent Instructions

## Scope
Applies to files under `tests/`.

## Style
- Use descriptive test names that begin with `test_`.
- Keep each test focused on a single assertion theme.
- Avoid flaky timing or external dependency assumptions.

## Coverage Targets
- Prefer broad scenario coverage with small unit checks.
- Include both positive and negative validation cases where practical.
